# Administrative rendition artifacts

These `AXX` bundle builders and the retained A12 ZIP were created for technical
rendition/reporting. They are preserved exactly for traceability but are not
part of the authoritative calibration, estimability or model-based DOE
pipeline.

The scripts were moved from the `fermentation_model/` root on 2026-07-14. The
complete former repository-root `rendicion_tecnica/` tree is preserved in
`technical_reports/`. These are frozen snapshots and may contain path
assumptions from their original location. Do not use them as model entry points.
